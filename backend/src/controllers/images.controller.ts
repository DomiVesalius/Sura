import { Request, Response, NextFunction, RequestHandler } from "express";

const NAMESPACE = "Images Controller";

export const addImage: RequestHandler = (req: Request, res: Response, next: NextFunction) => {};

export const deleteImage: RequestHandler = (req: Request, res: Response, next: NextFunction) => {};
